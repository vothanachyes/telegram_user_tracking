# =============================================================================
# Telegram User Tracking - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# =============================================================================
# DEPLOYMENT SCRIPT CONFIGURATION
# =============================================================================
# Required for automated deployment script (scripts/deploy_update.py)

# GitHub Personal Access Token (required for GitHub operations)
# Get from: GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
# Scope needed: repo (full control of private repositories)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Public Releases Repository (where binaries are published)
# This should be your PUBLIC repository for releases
GITHUB_REPO_OWNER=your_username
GITHUB_REPO_NAME=telegram_user_tracking-releases

# Private Repository (for CI/CD - Mac only, optional)
# Only needed if you want automatic Windows builds via CI/CD from Mac
GITHUB_PRIVATE_REPO_OWNER=your_username
GITHUB_PRIVATE_REPO_NAME=telegram_user_tracking

# GitHub Workflow Name (optional, has default)
# Name of the workflow file that builds Windows executable
GITHUB_WORKFLOW_NAME=Build Windows Executable

# =============================================================================
# FIREBASE CONFIGURATION
# =============================================================================
# Required for Firebase authentication and update system

# Path to Firebase service account credentials JSON file
# Download from: Firebase Console → Project Settings → Service Accounts
FIREBASE_CREDENTIALS_PATH=/path/to/firebase-credentials.json

# Firebase Project ID (optional, for reference)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase Web API Key (optional, for reference)
FIREBASE_WEB_API_KEY=your-firebase-web-api-key

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application metadata and settings

# Application Name
APP_NAME=Telegram User Tracking

# Application Version (used in builds and updates)
APP_VERSION=1.0.0

# Developer Information
DEVELOPER_NAME=Vothana CHY
DEVELOPER_EMAIL=vothanachy.es@gmail.com
DEVELOPER_CONTACT=+85510826027

# =============================================================================
# PATHS (Optional - defaults provided)
# =============================================================================
# Custom paths for development (production uses secure user directories)

# Database path (optional, defaults to user data directory)
# DATABASE_PATH=./data/app.db

# Download directory (optional, defaults to user data directory)
# DEFAULT_DOWNLOAD_DIR=./downloads

# =============================================================================
# THEME & UI SETTINGS (Optional)
# =============================================================================
# Customize application appearance

# Primary color (hex code)
PRIMARY_COLOR=#082f49

# Splash screen duration in seconds (optional)
# SPLASH_SCREEN_DURATION=2.0

# =============================================================================
# TELEGRAM SETTINGS (Optional)
# =============================================================================
# Telegram API configuration

# Device model for Telethon (optional)
# TELEGRAM_DEVICE_MODEL=ESC_APP